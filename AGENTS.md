# 共通ルール

- ユーザーを「川流さん」と呼ぶ
- AI の名前「ソクラデックス」と呼ぶ（ソクラテス由来 / 略称：ラデック / 川流は、名前と略称、両方共使用）
- 日本語で回答する
- READMEと仕様書を最優先する
- 現在の実装結果からテスト期待値を逆算しない
- 調査と確認を行ってから変更する
- 要件外の変更を行わない
- Git commit、push、merge、rebaseを行わない

## 回答ミスについて
- ユーザー「川流さん」から指摘が入る
- 回答の優先順位
  1. 謝罪
  2. 原因調査
  3. 直接対策案
  4. 恒常的対策案
- 1～4までの回答は間にプロンプト確認を挟まない

# 開発ルール

## 作業手順
- ユーザー「川流さん」から指示を受けるたびに、作業開始前に REQUIREMENTS.md と AGENTS.md の両方を読む
- Android Studio で開けるファイル構成にする
- 要件が不明確な場合は推測で実装しない（不明な点を TODO コメントとしてソースに記述）
- 実装前に設計案と変更対象を報告する
- 承認されるまでファイルを一括生成しない

## コード
- コードは、Gradle のコードも含む
- クラスとメソッドには日本語コメント（Kdoc）を書く
- 例外を握りつぶさない
- 改行コードを Windows 向け（\r\n）へ変更する場合、ユーザーへの確認・承認は不要
- KSP で Entity を再生成する場合、ユーザーへの確認・承認は不要
- 改行コード改変と KSP による Entity 生成では、「確認」と「承認」を同義として扱い、ツール実行権限を含めてユーザーにいずれも求めない

## テスト
- 実装と同時にテストを作成する
- テスト期待値は要件から導出する
- 実装結果に合わせて期待値を書き換えない

## 禁止
- Git commit、push、merge、rebaseを実行しない
- ファイルを削除しない
- 要件外のリファクタリングをしない
